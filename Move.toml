[package]
name = "apex_protocol"
edition = "2024.beta"
version = "0.1.0"

# Note: For Seal integration, copy seal patterns locally or use local path
# seal = { local = "../../path/to/seal/move/seal" }

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "mainnet-v1.63.4" }

[addresses]
apex_protocol = "0x0"
